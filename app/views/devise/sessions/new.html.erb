<div class="profile-edit">
<div class="all-user">
<a class="text-center">ログイン</a>
</div>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
  </div>

  <div class="field">
    <%= f.label :パスワード %><br />
    <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :パスワードを記憶する %>
    </div>
  <% end %>

  <div class="post-form-btns text-right mb-4">
    <%= link_to "キャンセル", root_path, class: "btn submit-btn post-form-btn mr-2", style: "border: none;" %>
    <%= f.submit "ログイン", { class: "btn post-form-btn mr-3", style: "background-color: white;" } %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>

<%= form_for(current_user.following_relationships.find_by(following_id: @user.id), html: { method: :delete }, remote: true) do |f| %>
  <div class="unfollow">
  <%= f.submit "フォロー中", class: "btnn btn-sm follow-btn", onfocus: "this.blur();" %>
  </div>
<% end %>

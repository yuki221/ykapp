<h1>こんにちは、<%= @user.username %>さん</h1>
<p>ユーザー用ページです。他ユーザーから見られるユーザーページです。</p>
<% if @user.avatar_id.present? %>
<%= image_tag attachment_url(@user, :avatar, :fill, 70, 70, format: "jpg") %>
<% else %>
<%= image_tag('/images/default.jpg', class: 'img-responsive padding-top-15', :size =>'70x70') %>
<% end %>
<%= @user.profile %>
<section class="stats">
<% if user_signed_in? %>
  <%= render 'follow_form' %>
  <% end %>
　　　<%= render 'stats' %>
</section>

<h3>いいねしている投稿</h3>
<% @user.liked_posts.each do |post| %>
  <%= post.title %>
  <% if post.image_id.present? %>
        <div class="post-img">
          <%=link_to (image_tag attachment_url(post, :image, :fill, 350, 250,  format: "jpg")), "/posts/show/#{post.id}", class: 'post-photo' %>
          </div>
          <% else %>
<%=link_to (image_tag('/images/default.jpg',size: '350x250', class: 'img-responsive padding-top-15')), "/posts/show/#{post.id}", class: 'post-photo' %>
<% end %>
<% end %>

<!-- テスト -->
<ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
  <li class="nav-item">
    <%= link_to "#", { class: "nav-link d-flex align-items-center justify-content-center px-0", id: "pills-post-tab", href: "#pills-post",
          role: "tab", "data-toggle" => "pill", "aria-controls" => "pills-post", "aria-selected" => "true"} do %>
        <div class="user-status">
          <div class="status-name p-0">投稿</div>
          <div class="status-value"><%= @user.posts.count.to_s(:delimited) %></div>
        </div>
      <% end %>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="pills-profile-tab" data-toggle="pill" href="#pills-profile" role="tab" aria-controls="pills-profile" aria-selected="false">Profile</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="pills-contact-tab" data-toggle="pill" href="#pills-contact" role="tab" aria-controls="pills-contact" aria-selected="false">Contact</a>
  </li>
</ul>
<!-- 上はnavbar -->

<div class="tab-content" id="pills-tabContent">
  <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
  <div class="tab-pane fade" id="pills-post" role="tabpanel" aria-labelledby="pills-post-tab">
    <% @user.posts.each do |post| %>
      <%= post.title %>
      <% if post.image_id.present? %>
        <div class="post-img">
          <%=link_to (image_tag attachment_url(post, :image, :fill, 350, 250,  format: "jpg")), "/posts/show/#{post.id}", class: 'post-photo' %>
        </div>
      <% else %>
        <%=link_to (image_tag('/images/default.jpg',size: '350x250', class: 'img-responsive padding-top-15')), "/posts/show/#{post.id}", class: 'post-photo' %>
      <% end %>
    <% end %>
  </div>
  </div>

  <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab">
  <% @user.liked_posts.each do |post| %>
        <%= post.title %>
        <% if post.image_id.present? %>
          <div class="post-img">
            <%=link_to (image_tag attachment_url(post, :image, :fill, 350, 250,  format: "jpg")), "/posts/show/#{post.id}", class: 'post-photo' %>
          </div>
        <% else %>
          <%=link_to (image_tag('/images/default.jpg',size: '350x250', class: 'img-responsive padding-top-15')), "/posts/show/#{post.id}", class: 'post-photo' %>
        <% end %>
      <% end %>
  </div>

  <div class="tab-pane fade" id="pills-contact" role="tabpanel" aria-labelledby="pills-contact-tab">

    <% if @following.any? %>
    <% @user.followings.each do |user| %>
<table>
<tr>
  <td>  <%= user.username %> </td>
  <td><%= link_to '詳細', user_path(user) %></td>
</tr>

</table>
<% end %>

        <% else %>
      <div class="no-contents d-flex align-items-center justify-content-center mx-auto">
        誰もフォローしていません
      </div>
    <% end %>

  </div>

</div>


<!-- /テスト -->
